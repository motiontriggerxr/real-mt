const start=Date.now(),clock=document.getElementById("clock");function tick(){const d=new Date();clock.textContent=[d.getHours(),d.getMinutes(),d.getSeconds()].map(x=>String(x).padStart(2,"0")).join(":");document.getElementById("uptime").textContent=Math.floor((Date.now()-start)/1000)+"s"}setInterval(tick,1000);tick();if(navigator.getBattery)navigator.getBattery().then(b=>{const u=()=>document.getElementById("battery").textContent=Math.round(b.level*100)+"%";u();b.addEventListener("levelchange",u)});let v=+localStorage.getItem("real_mt_visits")||0;v++;localStorage.setItem("real_mt_visits",v);document.getElementById("visits").textContent=v;function toast(t){const e=document.getElementById("toast");e.textContent=t;e.classList.add("show");setTimeout(()=>e.classList.remove("show"),1600)}const c=document.getElementById("network"),x=c.getContext("2d");let n=[];function size(){c.width=innerWidth*devicePixelRatio;c.height=innerHeight*devicePixelRatio;x.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);n=Array.from({length:55},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,vx:(Math.random()-.5)*.2,vy:(Math.random()-.5)*.2}))}function draw(){x.clearRect(0,0,innerWidth,innerHeight);n.forEach(a=>{a.x+=a.vx;a.y+=a.vy;if(a.x<0||a.x>innerWidth)a.vx*=-1;if(a.y<0||a.y>innerHeight)a.vy*=-1});for(let i=0;i<n.length;i++)for(let j=i+1;j<n.length;j++){let a=n[i],b=n[j],d=Math.hypot(a.x-b.x,a.y-b.y);if(d<155){x.globalAlpha=(1-d/155)*.25;x.beginPath();x.moveTo(a.x,a.y);x.lineTo(b.x,b.y);x.strokeStyle="#397ff5";x.stroke()}}n.forEach(a=>{x.globalAlpha=.55;x.fillStyle="#20e66f";x.beginPath();x.arc(a.x,a.y,2,0,7);x.fill()});x.globalAlpha=1;requestAnimationFrame(draw)}addEventListener("resize",size);size();draw();